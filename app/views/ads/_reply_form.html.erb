<div id="reply">
<%= form_for([ad, ad.replies.new]) do |f| %>
  <div class="field">
    <%= f.label :body, 'Reply' %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
</div>
